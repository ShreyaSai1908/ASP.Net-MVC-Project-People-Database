@model CreateLanguageViewModel

@{
    ViewData["Title"] = "Edit Language";
}

<h3>Edit Language</h3>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditLanguage">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input asp-for="LanguageID" class="form-control" type="hidden" />
                <span asp-validation-for="LanguageID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LanguageName" class="control-label"></label>
                <input asp-for="LanguageName" class="form-control" />
                <span asp-validation-for="LanguageName" class="text-danger"></span>
            </div>
            <!--
                <div class="form-group">
                    <label asp-for="PeopleList" class="control-label"></label>
                    <select asp-for="PeopleList" class="form-control">
                        @*
                            @foreach (var item in Model.PeopleList)
                            {
                                <option value="@item.PersonID">@item.FirstName @item.LastName</option>
                            }
                        *@
                    </select>
                    <span asp-validation-for="PeopleList" class="text-danger"></span>
                </div>
            -->
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="ShowLanguage">Back to List</a>
</div>

